# Atualização diária

Execute `node scripts/update-unitel-gossip.mjs` para coletar títulos e resumos curtos das categorias públicas da Unitel. O script cria um arquivo por data em `archive/` e atualiza `LATEST.md`.
